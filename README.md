# Raspi Cat Sentry
A custom computer vision system running on Raspberry Pi that uses a fine-tuned YOLO26n model to detect a specific cat and send notifications only when the cat is waiting at the door.
